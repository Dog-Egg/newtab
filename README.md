# Browser Tab

A minimal React + TypeScript browser extension that replaces the browser new tab page.

## Scripts

```sh
npm install
npm run build
```

Load the generated `dist` directory as an unpacked extension in Chrome or another Chromium-based browser.
