{
  "name": "@browser-tab/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:extension": "pnpm --filter @browser-tab/extension build && node scripts/sync-extension.mjs",
    "dev": "pnpm prepare:extension && astro dev",
    "build": "pnpm prepare:extension && astro check && astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "autoprefixer": "^10.5.2",
    "postcss": "^8.5.16",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.7.2"
  }
}
