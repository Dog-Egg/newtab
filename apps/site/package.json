{
  "name": "@browser-tab/site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:app": "pnpm --filter @browser-tab/app build:web && node scripts/sync-app.mjs",
    "dev": "pnpm prepare:app && astro dev",
    "build": "pnpm prepare:app && astro check && astro build",
    "typecheck": "astro check",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^7.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "autoprefixer": "^10.5.2",
    "postcss": "^8.5.16",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.7.2"
  }
}